FUNCTION palindrome(s : STRING[50], start, end : INTEGER ) : BOOLEAN

BEGIN
    IF (start >= end) THEN
        return TRUE;
    ELSE
        palindrome(s, start+1, end-1);
    ELSE
        return FALSE;
    END_IF
END